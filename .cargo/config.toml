# Do not force a repository-wide Windows Rust target here.
#
# CPython from python.org normally needs the MSVC target, while MSYS2 Python
# normally needs the GNU target. Pick the target in the invoking script or in a
# per-user Cargo config instead of committing one default for every machine.
